# SYNTAX

## VARIABLES
`{var}` : use var value in html parts  

## OPERATORS
`&&` : and  
*	e.g.: `{VAR && <TAG/>}` : only print TAG if(VAR)  

## HTML
`<> </>` : **fragment** (empty tag)  
*	e.g.: `{<>{}</>}` is valid, `{{}}` not  